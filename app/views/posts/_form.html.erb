<%= form_for @post do |f| %>
  <% if @post.errors.any? %>
  <h2><%= pluralize(@post.errors.count, "erreur") %> empêche(nt) d'enregistrer cet article</h2>
    <ul>
      <% @post.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
      <% end %>
    </ul>
  <% end %>

    <%= f.label :titre %> : <br>
    <%= f.text_field :title %>
    <br>
    <br>
    <%= f.label :contenu, "Rédigez votre article ici :" %> <br>
    <%= f.text_area :content %>
    <br>
    <br>
    <%= f.submit %>


<% end %>
